---
description: Generate a self-contained HTML report explaining the current project (tech stack, architecture, methodologies, pitfalls) with parallel Technical and Layman views.
argument-hint: "[--depth=quick|standard|deep] [--mode=technical|layman|both] [--out=PATH] [--no-open]"
---

# /under-the-hood

<!-- SLOT: SlashCommand — Phase 4 logic-agent fills this stub with the full prompt that drives Claude through:
       1. Read schema/stack-detection.json and detect the project's stack
       2. Read manifest, key configs, entry points (capped per --depth)
       3. Produce JSON conforming to schema/analysis.schema.json (technical + layman variants)
       4. Write JSON to a temp path
       5. Invoke scripts/launch.sh
     See .framework/contracts/logic/SlashCommand.contract.md for the authoritative spec.
-->

This is a Phase 3 skeleton stub. The full slash command prompt has not been written yet.

When asked to run `/under-the-hood`, reply: "waid-commands skeleton — slash command not yet implemented. See .framework/contracts/logic/SlashCommand.contract.md for the planned behavior."
